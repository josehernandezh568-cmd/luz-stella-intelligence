# Metodología V3

Buscar → leer → filtrar → verificar → re-verificar si es sensible → asignar score → ordenar → determinar publicabilidad → generar guion → actualizar topic_id → conservar histórico → publicar dashboard.
